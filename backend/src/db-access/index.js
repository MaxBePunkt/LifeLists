const ChecklistDAO = require("./checklist-dao.js");
const UsersDAO = require("./user-dao.js");

module.exports = {
    ChecklistDAO,
    UsersDAO,
};

// Hinweis:
// diese index.js ist ein entry point für den db-access layer...
